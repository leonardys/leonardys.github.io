---
title: "Relearning Programming"
subtitle: My first impression of Structure and Interpretation of Computer Programs (SICP).
author: "Leonard Yulianus"
date: "12/13/2025"
categories: [programming]
image: "SICP.jpg"
---

## Why Though?
I've read many great things about the Structure and Interpretation of Computer Programs (SICP), and since it's [available for free](https://web.mit.edu/6.001/6.037/sicp.pdf), I decided to dive in.

SICP is a legendary computer science textbook, originally used to teach programming at MIT.
The book aims to teach you *how to think* about programming.
It introduces powerful abstractions and design principles through the Scheme programming language, a minimalist dialect of Lisp.

The book has inspired many programmers (beginners and experienced alike) to revisit the basics and understand programming at a deeper level, beyond mere syntax and frameworks.
They mention it as transformative, as something that fundamentally changed how they approach problem-solving.
For that reason alone, it seemed worth the time.

## My Initial Impression
I'm still early in the first chapter, having just finished the first section titled "The Elements of Programming."
Despite being new to Scheme, the text is surprisingly easy to follow.
Yet, two things have been genuinely awkward as I adjust.

The first is getting used to writing code in prefix notation [^prefix-notation].
In most languages, we write expressions in infix notation, such as `1 + 2 * 3`.
But in Scheme, we write the same expression as `(+ 1 (* 2 3))`.
The operator comes first, followed by its operands.
It feels alien to what I'm used to.
However, once I get over this, I can see the benefits of the consistency and simplicity of the notation.
The language design explicitly requires you to use parentheses to denote procedure application, which forces you to specify the exact order of operations.
Taking out the guesswork from reading code.

The second is the ever-present parentheses.
At first glance, Scheme code looks like a mountain of nested parentheses, which can be overwhelming.
However, modern text editors usually have the capabilities to highlight matching pairs and to automatically close opening parentheses.

## Trying It Out
I tried to do Problem Zero of Project Euler (the one presented on the register page).

::: {.callout}
A number is a perfect square, or a square number, if it is the square of a positive integer.
For example, $25$ is a square number because $5^2 = 5 \times 5 = 25$; it is also an odd square.

The first 5 square numbers are: $1, 4, 9, 16, 25$, and the sum of the odd squares is $1 + 9 + 25 = 35$.

Among the first **658 thousand** square numbers, what is the sum of all the odd squares?
:::

In Python, I can easily come up with the solution.
Looping through odd numbers, as only the squares of odd numbers can be odd.
```python
def sum_odd_squares(n):
	sum_of_squares = 0
	for i in range(1, n + 1, 2):
		sum_of_squares += i * i

	return sum_of_squares

# Call the function
print(sum_odd_squares(658000))
```

For Scheme, I had to rethink the solution and utilize recursion.
After considerably longer time, I came up with this solution.
```scheme
(define (sum-odd-squares start end sum)
  (if (> start end)
    sum
    (sum-odd-squares
      (+ start 2)
      end
      (+ sum (* start start)))))

; Call the procedure
(display (sum-odd-squares 1 658000 0))
```

## My Expectation
I don't expect to become an expert in Scheme or use it professionally at work.
What matters is the knowledge SICP offers: a deeper understanding of computation, system design, and mastery of abstraction and problem-solving techniques.
If reading SICP shifts how I approach problem-solving and makes me a more thoughtful programmer, then the time spent learning Scheme will have been worthwhile.
The language is merely the means; clarity of thinking is the ultimate end.

[^prefix-notation]: Placing the operator to the left of the operands
